# 📖 تعليمات إعداد المشروع على GitHub

## 🎯 الهدف
إنشاء مستودع GitHub سيقوم بتحديث قوائم IPTV تلقائياً كل 30 دقيقة للحفاظ على التوكنات صالحة دائماً.

## 📋 المتطلبات
- حساب على GitHub (مجاني)
- Git مثبت على جهازك
- اتصال بالإنترنت

## 🚀 خطوات الإعداد

### 1. إنشاء مستودع جديد على GitHub

1. اذهب إلى [github.com/new](https://github.com/new)
2. أدخل اسم للمستودع (مثلاً: `arabic-iptv-playlists`)
3. اجعله **Public** (أو Private إذا فضّلت)
4. اضغط على **Create repository**

### 2. رفع المشروع إلى GitHub

#### على Windows:
1. افتح CMD أو PowerShell في مجلد المشروع
2. شغّل أولاً ملف `setup_github.bat`:
   ```cmd
   setup_github.bat
   ```
3. ثم نفذ الأوامر التالية (استبدل `YOUR_USERNAME` و `REPO_NAME`):
   ```cmd
   git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
   git branch -M main
   git push -u origin main
   ```

#### على Linux/Mac:
1. افتح Terminal في مجلد المشروع
2. شغّل أولاً:
   ```bash
   chmod +x setup_github.sh
   ./setup_github.sh
   ```
3. ثم نفذ الأوامر التالية:
   ```bash
   git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
   git branch -M main
   git push -u origin main
   ```

### 3. تفعيل GitHub Actions

بعد رفع المشروع بنجاح:

1. اذهب إلى صفحة المستودع على GitHub
2. اضغط على **Actions** tab
3. إذا طلب الإذن، اضغط على **I understand my workflows, go ahead and enable them**
4. ستجد Workflow باسم "تحديث قوائم IPTV تلقائياً"
5. سيبدأ تشغيله تلقائياً كل 30 دقيقة

### 4. استخدام القوائم المحدثة

#### الطريقة 1: مباشرة من GitHub
1. اذهب إلى صفحة المستودع
2. افتح ملف `ELAHMAD_LIVE_CLEAN_100%.m3u`
3. اضغط على **Raw** للحصول على الرابط المباشر
4. استخدم هذا الرابط في مشغلك المفضل

#### الطريقة 2: تنزيل الملفات
1. اذهب إلى صفحة المستودع
2. اضغط على **Code** ثم **Download ZIP**
3. استخرج الملفات واستخدمها محلياً

## 🔧 خيارات إضافية

### تغيير فترة التحديث

لتغيير فترة التحديث من 30 دقيقة إلى فترة أخرى:

1. افتح ملف `.github/workflows/update-playlists.yml`
2. عدّل سطر cron:
   ```yaml
   # كل 15 دقيقة
   - cron: '*/15 * * * *'
   
   # كل ساعة
   - cron: '0 * * * *'
   
   # كل 6 ساعات
   - cron: '0 */6 * * *'
   ```
3. Commit التغييرات وارفعها

### التشغيل اليدوي

يمكنك تشغيل التحديث يدوياً في أي وقت:

1. اذهب إلى **Actions** tab
2. اختر Workflow "تحديث قوائم IPTV تلقائياً"
3. اضغط على **Run workflow** ثم **Run workflow**

## 📊 مراقبة التحديثات

- اذهب إلى **Actions** tab لرؤية سجل التحديثات
- ستظهر جميع التحديثات الأخيرة مع التواريخ
- يمكن رؤية تفاصيل كل عملية تحديث

## 🎯 استخدام القوائم في مشغلات IPTV

### VLC Media Player
1. افتح VLC
2. اضغط `Ctrl + N` أو File → Open Network Stream
3. أدخل رابط ملف M3u المباشر من GitHub
4. اضغط Play

### IPTV Smarters / TiviMate
1. أضف قائمة تشغيل جديدة
2. أدخل رابط M3U من GitHub
3. القناة ستتحدث تلقائياً كل 30 دقيقة

### Web Player
استخدم رابط GitHub Raw مباشرة في أي مشغل ويب يدعم M3U.

## ⚠️ استكشاف الأخطاء

### Workflow لا يعمل
- تأكد من أن GitHub Actions مفعّل في إعدادات المستودع
- تحقق من ملف YAML للتحقق من عدم وجود أخطاء

### القنوات لا تعمل
- بعض القنوات قد تكون محجوبة جغرافياً
- تحقق من اتصال الإنترنت
- انتظر التحديث التلقائي القادم

### تحديثات متكررة جداً
- عدّل cron schedule في ملف YAML
- أو أوقف الـ Workflow مؤقتاً من Actions tab

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من سجل Actions في GitHub
2. راجع تعليمات الإعداد هذه
3. تأكد من صلاحية اتصال الإنترنت

## 🎉 مبروك!

الآن لديك نظام تحديث تلقائي لقوائم IPTV العربية! ستظل القنوات محدثة دائماً دون تدخل يدوي.